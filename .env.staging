# Staging defaults (secrets go in .env.staging.local — gitignored)
VITE_APP_ENV=staging
WORKER_NAME=ai-tensorview-staging
# Optional custom domain for staging (leave empty to use *.workers.dev only):
# CUSTOM_DOMAIN=staging.example.com
# Override smoke test base URL if needed:
# SMOKE_TEST_URL=https://ai-tensorview-staging.your-subdomain.workers.dev
